services:
  mp-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mp-frontend
    ports:
      - "5173:5173"
    env_file:
      - .env
    restart: unless-stopped
